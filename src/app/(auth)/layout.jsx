export const metadata = {
	title: 'Auth • ShopMate AI',
};

export default function AuthLayout({ children }) {
	return <div className="min-h-screen grid place-items-center bg-gradient-to-br from-slate-50 to-slate-100 p-4">{children}</div>;
}
